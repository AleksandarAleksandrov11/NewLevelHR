/** Namespace "cookies" — English is the type reference for de/ and bg/. */
export const cookies = {
  meta: { title: 'TODO', description: 'TODO' },
} as const;
export type CookiesDict = typeof cookies;
