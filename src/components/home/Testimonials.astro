---
/** STUB — to be replaced by the section builder. */
import { type Locale } from '@/i18n/config';
interface Props { lang: Locale }
const { lang } = Astro.props;
---
<section class="section container-site" data-stub="Testimonials">
  <p class="eyebrow">Testimonials</p>
  <p class="mt-2 text-slate">Section "Testimonials" is being built ({lang}).</p>
</section>
