---
/** STUB — to be replaced by the page builder. */
import Placeholder from '@/views/Placeholder.astro';
import type { Locale } from '@/i18n/config';
import type { PageKey } from '@/i18n/routes';
interface Props { lang: Locale; pageKey: PageKey | 'blogPost'; postId?: string; path?: string; alternates?: Record<Locale, string> }
const { lang, pageKey, path, alternates } = Astro.props;
---
<Placeholder lang={lang} view={pageKey} path={path} alternates={alternates} />
